<%= form_with(model: stss_trade, local: true) do |form| %>
  <% if stss_trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stss_trade.errors.count, "error") %> prohibited this stss_trade from being saved:</h2>

      <ul>
      <% stss_trade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
